# Design Archive

Place the approved design archive here:

```text
design/guitar-tuner-design-2026-06-04.zip
```

The archive is ignored by git and must stay untracked. For verifier runs that use a
read-only archive outside the checkout, set `APERTUNE_DESIGN_ARCHIVE` to that file.

Expected SHA-256:

```text
c7caedef1dfaae78d450164bf041fc7f00f63f7632749624000084fed14c6382
```

Approved visual reference inside the archive:

```text
Tuner - Studio.html
```
